# nix2 live bot deps (Python 3.10+)
websockets>=12.0          # fast spot feed (Binance/Coinbase WS)
py-clob-client>=0.19.0    # Polymarket order placement (LIVE mode only)
# stdlib used for the rest: asyncio, json, urllib, ssl, math, bisect
